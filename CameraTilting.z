class CameraTilting : ZilchComponent
{
    function Initialize(init : CogInitializer)
    {
        //Zero.Connect(this.Space, Events.LogicUpdate, this.OnLogicUpdate);
        var actionSequence = Action.Sequence(this.Owner.Actions);
        //Action.Delay(actionSequence, 5);
        //Action.Property(actionSequence, @this.Owner.Transform.Size, Real(40), 1.0, Ease.Linear);
    }

    function OnLogicUpdate(event : UpdateEvent)
    {
    }
}
